---
name: ansible-lint

on:
  pull_request:
    branches:
      - main
    paths:
      - "**/*.yaml"

jobs:
  ansible-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run ansible-lint
        uses: ansible/ansible-lint@v26
